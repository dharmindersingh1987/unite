%%%%%%%%%%%%
mkdir unite
cd unite
python -m venv .venv
source .venv/bin/activate
pip install django
django-admin --version
django-admin startproject config .
python manage.py runserver
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
python manage.py startapp accounts
%%%%% register the app in settings
python manage.py check
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% in accounts/models.py
from django.db import models


class PacketUser(models.Model):
    pkt_id = models.IntegerField(primary_key=True)
    user_id = models.CharField(max_length=100)
    password = models.CharField(max_length=255)

    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        db_table = "packet_users"
        ordering = ["pkt_id"]

    def __str__(self):
        return f"{self.pkt_id} - {self.user_id}"
        
%%%%%%%%%% 
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser

%%%%%%%% Register the Model
% accounts/admin.py

from django.contrib import admin
from .models import PacketUser


@admin.register(PacketUser)
class PacketUserAdmin(admin.ModelAdmin):
    list_display = (
        "pkt_id",
        "user_id",
        "password",
        "created_at",
    )

    search_fields = (
        "pkt_id",
        "user_id",
    )
    
    readonly_fields = (
    "pkt_id",
    "user_id",
    "password",
    "created_at",
    )

    ordering = ("pkt_id",)

    list_per_page = 50


%%%%%%%%%%%%%%%%%%%%%%%%%%%
pip install django-import-export
% register 'import_export' in settins.py
%%%%%%%%%%%%%%%%%%%%%%%%%%%
% create accounts/resources.py


from import_export import resources
from .models import PacketUser


class PacketUserResource(resources.ModelResource):

    class Meta:
        model = PacketUser
        import_id_fields = ("pkt_id",)
        fields = (
            "pkt_id",
            "user_id",
            "password",
            "created_at",
        )
        
        
        
        
% modify accounts/admin.py


from django.contrib import admin
from import_export.admin import ImportExportModelAdmin

from .models import PacketUser
from .resources import PacketUserResource


@admin.register(PacketUser)
class PacketUserAdmin(ImportExportModelAdmin):
    resource_class = PacketUserResource

    list_display = (
        "pkt_id",
        "user_id",
        "password",
        "created_at",
    )

    search_fields = (
        "=pkt_id",
        "user_id",
    )

    list_filter = (
        "created_at",
    )

    ordering = ("pkt_id",)

    list_per_page = 50
    
    
 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
 python manage.py runserver
 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
 






